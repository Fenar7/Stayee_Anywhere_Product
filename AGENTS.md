<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# STRICT BEHAVIORAL RULE: NEVER EXECUTE WITHOUT EXPLICIT PERMISSION
**CRITICAL:** NEVER assume permission to execute actions (like merging branches, pushing code, or making major changes) just because the user asks a clarifying question (e.g., "so we need to merge right?"). You must ALWAYS answer the question first and wait for the user to explicitly say "yes, do it" or "proceed" before executing. Do NOT go ahead and do stuff unless explicitly told to do so.
